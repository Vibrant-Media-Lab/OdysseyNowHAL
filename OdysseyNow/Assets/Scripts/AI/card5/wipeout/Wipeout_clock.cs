using UnityEngine;
using UnityEngine.AI;

public class Wipeout_clock : MonoBehaviour {
    public Transform target;
    public Transform clock;
    public GameObject ball;
    public int level;

    // Update is called once per frame
    private void Update() {
        switch (level) {
            case 1:
                break;
            case 2:
                break;
            case 3:
                break;
        }
    }
}
